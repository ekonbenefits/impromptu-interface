<Project>
    <PropertyGroup>
        <VersionPrefix>7.0.1</VersionPrefix>
        <Version Condition="'$(Configuration)'=='Release'">
            $(VersionPrefix)$(VersionSuffix)
        </Version>
    </PropertyGroup>
</Project>